# ميزات الرد التلقائي على التعليقات

## 📋 نظرة عامة

تم تحديث نظام الرد التلقائي ليدعم Facebook Graph API v24.0 مع إصلاح مشكلة الرسائل الخاصة وإضافة ميزة الإشارة (Mention).

---

## ✨ الميزات المتاحة

### 1️⃣ **الإعجاب بالتعليق** (`like_comment_enabled`)
- يقوم البوت بعمل إعجاب تلقائي على التعليق
- مفيد لإظهار أنك شاهدت التعليق

### 2️⃣ **الرد على التعليق** (`reply_to_comment_enabled`)
- الرد التلقائي على التعليق باستخدام قالب محدد
- يدعم المتغيرات الديناميكية في القالب
- **جديد**: يمكن إضافة إشارة (mention) للمعلق في بداية الرد

### 3️⃣ **الإشارة للمعلق** (`mention_enabled`)
- عند تفعيل هذا الخيار، يتم إضافة `@[USER_ID]` في بداية الرد
- يظهر للمعلق إشعار بأنه تم ذكره في التعليق
- **ملاحظة**: يعمل فقط إذا كان `reply_to_comment_enabled` مفعل

### 4️⃣ **إرسال رسالة خاصة** (`reply_to_private_message_enabled`)
- **تم الإصلاح**: الآن يعمل بشكل صحيح مع Graph API v24
- يرسل رسالة خاصة للمعلق عبر Messenger
- يستخدم `comment_id` للرد على التعليق

---

## 🎯 المتغيرات المتاحة في القوالب

يمكنك استخدام المتغيرات التالية في أي قالب:

| المتغير | الوصف | مثال |
|---------|-------|------|
| `{name}` | اسم المعلق | "أحمد محمد" |
| `{time}` | الوقت الحالي | "14:30" |
| `{date}` | التاريخ الحالي | "2025-11-30" |
| `{datetime}` | التاريخ والوقت | "2025-11-30 14:30:45" |

---

## 📝 أمثلة على القوالب

### مثال 1: رد بسيط مع إشارة
```
شكراً لتعليقك! سنرد عليك قريباً 😊
```
**مع mention_enabled**: `@[123456789] شكراً لتعليقك! سنرد عليك قريباً 😊`

### مثال 2: رد مع اسم المعلق
```
مرحباً {name}! شكراً لاهتمامك بمنتجاتنا 🎉
```
**النتيجة**: `مرحباً أحمد محمد! شكراً لاهتمامك بمنتجاتنا 🎉`

### مثال 3: رد مع الوقت
```
تم استلام طلبك في {time}، سنتواصل معك قريباً
```
**النتيجة**: `تم استلام طلبك في 14:30، سنتواصل معك قريباً`

### مثال 4: رسالة خاصة
```
مرحباً {name}! 

شكراً لتواصلك معنا. للحصول على مزيد من المعلومات، يرجى زيارة موقعنا أو الاتصال بنا على:
📞 0123456789
```

---

## 🔧 إعدادات المنشور

كل منشور في قاعدة البيانات يحتوي على الإعدادات التالية:

```php
[
    'enabled' => true,                              // تفعيل/تعطيل الرد التلقائي
    'like_comment_enabled' => true,                 // الإعجاب بالتعليق
    'reply_to_comment_enabled' => true,             // الرد على التعليق
    'mention_enabled' => true,                      // إضافة mention في الرد
    'reply_to_private_message_enabled' => false,    // إرسال رسالة خاصة
    'comment_reply_template' => 'نص الرد...',       // قالب الرد على التعليق
    'private_message_template' => 'نص الرسالة...', // قالب الرسالة الخاصة
    'keywords' => ['السعر', 'الباقات'],            // كلمات مفتاحية (اختياري)
    'exclude_keywords' => ['شكراً', 'تم']          // كلمات مستبعدة (اختياري)
]
```

---

## 🎮 منطق الكلمات المفتاحية

### الكلمات المفتاحية (`keywords`)
- **فارغة أو null**: يرد على جميع التعليقات
- **بها كلمات**: يرد فقط على التعليقات التي تحتوي على أي كلمة منها

**مثال**:
```php
'keywords' => ['السعر', 'الباقات']
```
- ✅ يرد على: "كم السعر؟"
- ✅ يرد على: "ما هي الباقات المتاحة؟"
- ❌ لا يرد على: "شكراً لكم"

### الكلمات المستبعدة (`exclude_keywords`)
- **فارغة أو null**: لا يستبعد أي تعليق
- **بها كلمات**: يتجاهل التعليقات التي تحتوي على أي كلمة منها

**مثال**:
```php
'exclude_keywords' => ['شكراً', 'تم']
```
- ❌ لا يرد على: "شكراً لكم"
- ❌ لا يرد على: "تم الاستلام"
- ✅ يرد على: "كم السعر؟"

---

## 🔐 الصلاحيات المطلوبة

تأكد من أن تطبيق Facebook لديه الصلاحيات التالية:

1. **`pages_messaging`** - لإرسال الرسائل الخاصة
2. **`pages_manage_metadata`** - لإدارة بيانات الصفحة
3. **`pages_read_engagement`** - لقراءة التعليقات والتفاعلات
4. **`pages_manage_engagement`** - للرد على التعليقات

### Webhook Subscriptions
تأكد من الاشتراك في:
- ✅ `feed` - للحصول على إشعارات التعليقات

---

## 🐛 استكشاف الأخطاء

### الرسائل الخاصة لا تصل
1. تحقق من صلاحية `pages_messaging`
2. تأكد من أن `page_access_token` صالح
3. تحقق من أن التعليق من مستخدم حقيقي (ليس من الصفحة)
4. الرسائل الخاصة تعمل فقط خلال 24 ساعة من التعليق

### الإشارة (Mention) لا تعمل
1. تأكد من تفعيل `mention_enabled`
2. تأكد من تفعيل `reply_to_comment_enabled` أيضاً
3. تحقق من أن `comment_reply_template` ليس فارغاً

### لا يرد على التعليقات
1. تحقق من أن `enabled = true`
2. تحقق من الكلمات المفتاحية والمستبعدة
3. راجع الـ logs في `storage/logs/laravel.log`

---

## 📊 سجلات النظام (Logs)

يتم تسجيل جميع الأحداث في `storage/logs/laravel.log`:

```
[2025-11-30 10:20:00] Processing comment: comment_id=123, from_name=أحمد
[2025-11-30 10:20:01] Liked comment: comment_id=123
[2025-11-30 10:20:02] Replied to comment: comment_id=123, mention_enabled=true
[2025-11-30 10:20:03] Sent private message: comment_id=123
[2025-11-30 10:20:04] Auto-reply completed successfully
```

---

## 🚀 التحديثات الأخيرة

### v2.0 (2025-11-30)
- ✅ إصلاح مشكلة الرسائل الخاصة مع Graph API v24
- ✅ دمج ميزة الإشارة (mention) مع الرد على التعليق
- ✅ إضافة متغير `{name}` في القوالب
- ✅ تحسين معالجة JSON في الطلبات
- ✅ إضافة دعم `page_id` في endpoint الرسائل

---

## 💡 نصائح

1. **اختبر القوالب**: جرب القوالب على منشور تجريبي أولاً
2. **استخدم المتغيرات**: اجعل الردود شخصية باستخدام `{name}`
3. **كن محدداً**: استخدم الكلمات المفتاحية لتحديد التعليقات المهمة
4. **راقب السجلات**: تابع الـ logs لمعرفة ما يحدث
5. **احترم الخصوصية**: لا ترسل رسائل خاصة إلا عند الضرورة
