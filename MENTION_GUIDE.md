# كيفية عمل الإشارة (Mention) في Facebook

## 🎯 المشكلة السابقة
كان يظهر النص `@[123456789]` كنص عادي ولا يعمل كـ mention قابل للنقر.

## ✅ الحل
Facebook Graph API يحتاج إلى معامل إضافي اسمه `message_tags` لتحويل النص إلى mention حقيقي.

---

## 📋 كيف يعمل الآن

### 1. **عند تفعيل `mention_enabled`**:
```php
mention_enabled = true
comment_reply_template = "شكراً لتعليقك!"
```

### 2. **يتم إرسال الطلب إلى Facebook بهذا الشكل**:
```php
POST /{comment-id}/comments
{
    "message": "@[123456789] شكراً لتعليقك!",
    "message_tags": "[{\"id\":\"123456789\",\"offset\":0,\"length\":13}]",
    "access_token": "PAGE_ACCESS_TOKEN"
}
```

### 3. **النتيجة**:
- ✅ يظهر اسم المستخدم كـ mention قابل للنقر
- ✅ يحصل المستخدم على إشعار
- ✅ عند النقر على الاسم، يذهب إلى بروفايل المستخدم

---

## 🔧 التفاصيل التقنية

### `message_tags` Format:
```json
[
    {
        "id": "USER_ID",           // معرف المستخدم
        "offset": 0,               // موضع البداية في النص (دائماً 0)
        "length": 13               // طول @[USER_ID]
    }
]
```

### مثال عملي:
```php
// المستخدم ID: 123456789
// النص: "@[123456789] مرحباً بك!"

message_tags = [
    {
        "id": "123456789",
        "offset": 0,              // يبدأ من أول حرف
        "length": 13              // طول "@[123456789]"
    }
]
```

---

## 📊 أمثلة

### مثال 1: رد بسيط مع mention
**الإعدادات**:
```php
mention_enabled = true
comment_reply_template = "شكراً لك!"
```

**ما يُرسل**:
```
Message: @[123456789] شكراً لك!
Tags: [{"id":"123456789","offset":0,"length":13}]
```

**ما يظهر للمستخدم**:
```
زكريا زكري شكراً لك!
```
(اسم "زكريا زكري" يكون أزرق وقابل للنقر)

---

### مثال 2: رد مع اسم المستخدم
**الإعدادات**:
```php
mention_enabled = true
comment_reply_template = "مرحباً {name}! كيف يمكننا مساعدتك؟"
```

**ما يُرسل**:
```
Message: @[123456789] مرحباً زكريا زكري! كيف يمكننا مساعدتك؟
Tags: [{"id":"123456789","offset":0,"length":13}]
```

**ما يظهر للمستخدم**:
```
زكريا زكري مرحباً زكريا زكري! كيف يمكننا مساعدتك؟
```
(الاسم الأول فقط يكون mention، الثاني نص عادي)

---

### مثال 3: بدون mention
**الإعدادات**:
```php
mention_enabled = false
comment_reply_template = "شكراً لتعليقك!"
```

**ما يُرسل**:
```
Message: شكراً لتعليقك!
Tags: (لا يوجد)
```

**ما يظهر للمستخدم**:
```
شكراً لتعليقك!
```
(نص عادي بدون mention)

---

## 🎨 الفرق البصري

### ❌ قبل الإصلاح:
```
@[123456789] شكراً لتعليقك!
```
(يظهر كنص عادي، غير قابل للنقر)

### ✅ بعد الإصلاح:
```
زكريا زكري شكراً لتعليقك!
```
(الاسم باللون الأزرق، قابل للنقر، يرسل إشعار)

---

## 🔍 كيف تتحقق من عمل الـ Mention

1. **افتح Facebook** وانتقل إلى المنشور
2. **ابحث عن الرد** من البوت
3. **تحقق من**:
   - ✅ هل الاسم باللون الأزرق؟
   - ✅ هل يمكن النقر عليه؟
   - ✅ هل يذهب إلى بروفايل المستخدم؟
   - ✅ هل حصل المستخدم على إشعار؟

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يزال يظهر كنص عادي
**الحلول**:
1. تأكد من أن `mention_enabled = true`
2. تأكد من أن `reply_to_comment_enabled = true`
3. تحقق من الـ logs: `storage/logs/laravel.log`
4. تأكد من أن `$fromId` ليس فارغاً

### المشكلة: يظهر خطأ في الـ API
**الحلول**:
1. تحقق من صلاحيات الـ Page Access Token
2. تأكد من أن الـ User ID صحيح
3. راجع الـ Facebook API response في الـ logs

---

## 📝 ملاحظات مهمة

1. **الـ mention يعمل فقط مع User IDs**، لا يعمل مع Page IDs
2. **الـ offset دائماً 0** لأن الـ mention في بداية الرسالة
3. **الـ length يُحسب تلقائياً** من طول `@[USER_ID]`
4. **يمكن إضافة mentions متعددة** في نفس الرسالة (لكن حالياً ندعم واحد فقط)

---

## 🚀 التحديثات

### v2.1 (2025-11-30)
- ✅ إصلاح مشكلة الـ mention ليظهر كـ tag قابل للنقر
- ✅ إضافة `message_tags` parameter
- ✅ حساب تلقائي لـ offset و length
- ✅ دعم تمرير `mentionUserId` إلى `replyToComment()`

---

## 💡 نصيحة

للحصول على أفضل تجربة، استخدم:
```php
mention_enabled = true
comment_reply_template = "شكراً لاهتمامك! سنرد عليك قريباً 😊"
```

سيظهر كـ:
```
زكريا زكري شكراً لاهتمامك! سنرد عليك قريباً 😊
```
مع إشعار للمستخدم! 🎉
