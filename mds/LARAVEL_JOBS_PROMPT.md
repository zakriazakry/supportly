# 📝 Prompt: كيفية عمل Laravel Jobs للردود التلقائية

## السياق (Context)

أنت تبني نظام ردود تلقائية على تعليقات Facebook باستخدام Laravel. المشكلة الرئيسية هي:

1. **Facebook API Rate Limiting**: 
   - كل Page Access Token له حد 200 طلب/ساعة
   - كل رد تلقائي يستخدم 1-3 طلبات (إعجاب + رد + رسالة خاصة)

2. **التحدي**:
   - مستخدم واحد قد يحتاج 200 طلب/ساعة
   - 10 مستخدمين = 2000 طلب/ساعة محتملة
   - خطر تجاوز الحد وإيقاف البوت

3. **المطلوب**:
   - نظام يعمل تحت الضغط بدون توقف
   - تأجيل الطلبات تلقائياً عند الوصول للحد
   - نافذة أمان (ساعة وربع بدلاً من ساعة)

---

## المتطلبات الوظيفية

### 1. معالجة غير متزامنة (Asynchronous Processing)
```
- استقبال Webhook من Facebook
- رد فوري خلال 20 ثانية (مهم!)
- إرسال المعالجة الفعلية إلى Queue
- عدم حظر الـ Webhook
```

### 2. Rate Limiting ذكي
```
- تتبع عدد الطلبات لكل مستخدم في الساعة الحالية
- الحد الأقصى: 200 طلب/ساعة (عادي)
- الحد الأقصى: 400 طلب/ساعة (مستخدمين مميزين)
- استخدام Cache للتخزين المؤقت
```

### 3. التأجيل التلقائي (Auto Delay)
```
- عند الوصول للحد الأقصى:
  * تأجيل الـ Job لمدة 75 دقيقة (ساعة وربع)
  * عدم فشل الـ Job
  * إعادة المحاولة تلقائياً
```

### 4. نظام الأولوية
```
- مستخدمين عاديين: تأخير 5-10 ثواني
- مستخدمين مميزين: معالجة فورية
- حد أعلى للمميزين (400 بدلاً من 200)
```

### 5. التحقق من القيود
```
- التحقق من الاشتراك النشط
- التحقق من القيود الشهرية
- التحقق من عدم الرد المسبق
- التحقق من الكلمات المطلوبة/المستبعدة
```

---

## البنية التقنية المطلوبة

### 1. Job Class
```php
class ProcessAutoReplyJob implements ShouldQueue
{
    use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;
    
    public $timeout = 120;
    public $tries = 3;
    public $backoff = [60, 180, 300];
    
    // البيانات المطلوبة
    protected $postId;
    protected $commentId;
    protected $commentText;
    protected $fromId;
    protected $fromName;
    protected $pageId;
    
    public function handle(FacebookLibsServices $service)
    {
        // 1. التحقق من البوست والصفحة
        // 2. التحقق من المستخدم والاشتراك
        // 3. التحقق من القيود الشهرية
        // 4. Rate Limiting Check
        // 5. تنفيذ الرد أو التأجيل
        // 6. تحديث العدادات
    }
}
```

### 2. Rate Limiting Logic
```php
// مفتاح Cache فريد لكل مستخدم ولكل ساعة
$rateLimitKey = "rate_limit:user:{$userId}:" . now()->format('Y-m-d-H');

// الحصول على العدد الحالي
$requestCount = Cache::get($rateLimitKey, 0);

// التحقق من الحد
if ($requestCount >= $maxRequestsPerHour) {
    // تأجيل 75 دقيقة
    $this->release(75 * 60);
    return;
}

// تنفيذ الطلبات
$requestsMade = 0;
// ... تنفيذ الإجراءات ...

// تحديث العداد (صالح لمدة 75 دقيقة)
Cache::put($rateLimitKey, $requestCount + $requestsMade, now()->addMinutes(75));
```

### 3. Webhook Controller
```php
public function receive(Request $request)
{
    $data = $request->all();
    
    // معالجة سريعة
    foreach ($data['entry'] as $entry) {
        // إرسال إلى Queue
        ProcessAutoReplyJob::dispatch(...)
            ->delay(now()->addSeconds($delay));
    }
    
    // رد فوري (مهم!)
    return response("OK", 200);
}
```

### 4. Queue Configuration
```env
QUEUE_CONNECTION=redis  # أو database للتطوير
QUEUE_FAILED_DRIVER=database
```

---

## آلية العمل خطوة بخطوة

### الخطوة 1: استقبال Webhook
```
1. Facebook يرسل إشعار بتعليق جديد
2. FacebookWebhookController يستقبل الطلب
3. التحقق السريع من البيانات الأساسية
4. إرسال Job إلى Queue
5. رد فوري لـ Facebook (OK 200)
```

### الخطوة 2: معالجة Job
```
1. Queue Worker يلتقط الـ Job
2. التحقق من البوست والصفحة
3. التحقق من المستخدم والاشتراك
4. التحقق من القيود الشهرية
5. Rate Limiting Check
```

### الخطوة 3: Rate Limiting Decision
```
إذا (الطلبات الحالية < الحد الأقصى):
    ✅ تنفيذ الرد
    ✅ تحديث العدادات
    ✅ حفظ حالة الرد
    
إذا (الطلبات الحالية >= الحد الأقصى):
    ⏸️ تأجيل الـ Job لمدة 75 دقيقة
    📝 تسجيل في Logs
    🔄 إعادة إلى Queue
```

### الخطوة 4: تنفيذ الرد
```
1. انتظار 1-2 ثانية (تجنب Rate Limit)
2. إعجاب بالتعليق (إذا مفعّل)
3. انتظار 1-2 ثانية
4. الرد على التعليق (إذا مفعّل)
5. انتظار 1-2 ثانية
6. إرسال رسالة خاصة (إذا مفعّل)
7. حفظ حالة الرد
8. تحديث عداد الردود الشهرية
```

---

## أمثلة السيناريوهات

### سيناريو 1: حمل خفيف
```
- مستخدم واحد
- 50 تعليق/ساعة
- كل رد = 3 طلبات
- إجمالي = 150 طلب/ساعة
- النتيجة: ✅ تنفيذ فوري (تحت الحد)
```

### سيناريو 2: حمل متوسط
```
- 5 مستخدمين
- 30 تعليق/ساعة لكل واحد
- كل رد = 3 طلبات
- إجمالي = 450 طلب/ساعة (موزعة)
- كل مستخدم = 90 طلب/ساعة
- النتيجة: ✅ تنفيذ فوري (كل واحد تحت حده)
```

### سيناريو 3: حمل ثقيل
```
- مستخدم واحد
- 100 تعليق/ساعة
- كل رد = 3 طلبات
- إجمالي = 300 طلب/ساعة
- النتيجة:
  * أول 66 تعليق → تنفيذ فوري (198 طلب)
  * باقي 34 تعليق → تأجيل للساعة التالية
```

### سيناريو 4: مستخدم مميز
```
- مستخدم مميز (Priority)
- 100 تعليق/ساعة
- كل رد = 3 طلبات
- الحد الأقصى = 400 طلب/ساعة
- النتيجة: ✅ تنفيذ فوري (300 < 400)
```

---

## الإعدادات المطلوبة

### 1. Queue Table (إذا استخدمت database)
```bash
php artisan queue:table
php artisan queue:failed-table
php artisan migrate
```

### 2. تشغيل Worker
```bash
# للتطوير
php artisan queue:work --queue=auto-replies

# للإنتاج
php artisan queue:work --queue=auto-replies --tries=3 --timeout=120 --sleep=3
```

### 3. Supervisor (للإنتاج)
```ini
[program:supportly-worker]
command=php /path/to/artisan queue:work --queue=auto-replies
numprocs=4
autostart=true
autorestart=true
```

---

## معايير النجاح

1. ✅ **لا توقف**: البوت يعمل 24/7 بدون توقف
2. ✅ **لا تجاوز**: عدم تجاوز Rate Limit أبداً
3. ✅ **معالجة ذكية**: تأجيل تلقائي عند الحاجة
4. ✅ **أولوية**: مستخدمين مميزين يحصلون على معالجة أسرع
5. ✅ **شفافية**: Logs واضحة لكل عملية
6. ✅ **موثوقية**: إعادة محاولة تلقائية عند الفشل

---

## الأخطاء الشائعة وحلولها

### خطأ 1: تجاوز Rate Limit
```
السبب: عدم التحقق قبل كل طلب
الحل: استخدام Cache للتتبع والتأجيل التلقائي
```

### خطأ 2: Webhook Timeout
```
السبب: معالجة طويلة في Controller
الحل: إرسال فوري للـ Queue والرد بـ OK
```

### خطأ 3: Jobs تفشل باستمرار
```
السبب: عدم معالجة الاستثناءات
الحل: try-catch + إعادة محاولة + failed() method
```

### خطأ 4: استهلاك ذاكرة عالي
```
السبب: عدد Workers كبير جداً
الحل: 2-4 workers كافية + max-jobs=1000
```

---

## الخلاصة

هذا النظام يضمن:
- 🚀 **أداء عالي**: معالجة آلاف التعليقات
- 🛡️ **حماية**: عدم تجاوز Rate Limits
- ⚡ **سرعة**: رد فوري لـ Facebook
- 🎯 **دقة**: التحقق من جميع القيود
- 📊 **مراقبة**: Logs شاملة
- 🔄 **موثوقية**: إعادة محاولة تلقائية

**استخدم هذا الـ Prompt كمرجع عند بناء أنظمة Jobs مشابهة!**
